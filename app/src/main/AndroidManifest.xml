<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.productcrudproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProductCrudProject">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginStudent" android:label="Login"/>
        <activity android:name=".RegisterStudent" android:label="Register"/>
        <activity android:name=".StudentList" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".AdminRegister" />
        <activity android:name=".StudentDashboard" />
        <activity android:name=".AdminDashboard" />
        <activity android:name=".AdminAddCourse" />
    </application>

</manifest>